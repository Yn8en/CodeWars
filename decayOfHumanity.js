let beginDecayOfHumanity = (soon) => {
    const problem = soon-1;
    let now = new Array(soon).fill(["with","problems"]);

    let creativity = (have, a, chance, though) => {
        if (problem == chance) {
            return "Hello World!"
        }
    }

    let us = now.reduce(creativity);
    return us;
}

console.log(beginDecayOfHumanity(69));